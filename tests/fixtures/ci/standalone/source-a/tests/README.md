# UGA-04 fixture suite map

Minimal stand-in for the real `tests/README.md`: just enough documented suite headings for the
documentation-drift rule to compare against `ci/gates.toml`'s multi-OS "core" gates.

## Suites

### platform

### fault-injection

### performance
