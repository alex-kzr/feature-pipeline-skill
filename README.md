# Feature Pipeline Skill

The portable feature-pipeline core lives in this independently versioned repository.

Implementation layout and contracts are introduced by the subsequent core-foundation tasks.
